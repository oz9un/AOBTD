# Default / commonly-guessed credentials. Industry-standard corpus.
#
# Sources: CIRT.net default-password DB, SecLists Passwords/Default-Credentials,
# OWASP Top 100 weak-password lists.
#
# Format: username:password (one per line, # for comments).
# The probe tries each pair against any discovered login endpoint.

# ── Classic admin defaults ────────────────────────────────────────────
admin:admin
admin:password
admin:admin123
admin:12345
admin:123456
admin:1234
admin:pass
admin:changeme
admin:letmein
admin:default

# ── Root / administrator ──────────────────────────────────────────────
root:root
root:toor
root:password
root:admin
administrator:administrator
administrator:password

# ── Common test / demo accounts ───────────────────────────────────────
test:test
demo:demo
guest:guest
user:user
user:password

# ── Vendor defaults (widely deployed devices / frameworks) ────────────
# CMS / app-level defaults
wordpress:wordpress
joomla:joomla

# Database GUI defaults
postgres:postgres
mysql:mysql
sa:sa

# Framework defaults (Tomcat, Jenkins, etc.)
tomcat:tomcat
jenkins:jenkins
manager:manager
kibana:kibana
elastic:changeme

# ── Email-as-username patterns (common in modern web apps) ────────────
# Probed with the target host filled in by the probe itself.
admin@example.com:admin
admin@example.com:admin123
admin@example.com:password
