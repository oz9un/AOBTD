{
  "name": "aobtd-cloudflare-oast",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets:configure": "node scripts/configure-secrets.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  },
  "overrides": {
    "undici": "7.29.0"
  }
}
